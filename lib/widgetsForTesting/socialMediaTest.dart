import 'package:flutter/material.dart';

class SocialMediaTest extends StatelessWidget {

  const SocialMediaTest();

  @override
  Widget build(BuildContext context) {
    return Container(
      color: const Color(0xFF2DBD3A),
      child: Text("this is a test")
    );
  }
}